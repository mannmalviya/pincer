// Placeholder — the compose page lives here once the agent's
// `generate-post` skill is wired up. See PLAN.md §"Skills to build".

import { PlaceholderSection } from "../_components/placeholder-section";

export default function ComposePage() {
  return (
    <PlaceholderSection
      label="Compose"
      title="Draft posts from a brief."
      body="This is where you'll paste a 3-line product brief, see platform-specific drafts side by side, edit them, and ship them to Reddit and Discord with one click. Wired up alongside the agent's generate-post and post-to-* skills."
    />
  );
}
